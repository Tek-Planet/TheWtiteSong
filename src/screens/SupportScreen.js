import React from 'react';
import {View, Text, StyleSheet, ScrollView} from 'react-native';

const SupportScreen = () => {
  return (
    <ScrollView>
      <View>
        <Text>Welcome to Support Screen</Text>
      </View>
    </ScrollView>
  );
};

export default SupportScreen;
